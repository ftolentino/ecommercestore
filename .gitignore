# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — real values live here and in Vercel env vars, never in the repo
.env.local
.env*.local

# Vercel CLI link state
.vercel

# Claude Code local state (shared .claude/settings.json stays tracked)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
